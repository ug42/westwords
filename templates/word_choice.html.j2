<div class="modal-body"> {# Changed span to div for proper block display #}
    <span>
    <b>
    Your role is {{player_role}}
    </b>
    </span>
    <span>
    {% if image|length %}<img class="role_image" src="{{ url_for('static', filename='img/' + image_theme + '/' + image) }}"
            title="{{role_description}}" width="24" height="24"><br> {% endif %}
    </span>
</div>
<div class="modal-body">
    Select a word:
</div>
<div class="modal-footer d-flex flex-wrap"> {# Added flex-wrap for button wrapping #}
    {% for w in words %}
    <button type="button" class="btn btn-outline-primary me-2 mb-2 close" onclick="send_word('{{game_id}}', '{{w}}')">{{w}}</button> {# Added me-2 mb-2 for spacing #}
    {% endfor %}
</div>