{% for p in players %}
<button class="btn btn-danger btn-sm" title="Remove Player"
    onclick="boot_player('{{ game_id }}', '{{ p.sid }}')">
    &times; {# Using a times symbol for 'close' #}
</button> &nbsp; {{ p.name }}<br>
{% endfor %}