<div class="mdl-dialog__content">
    You are a {{role}}.
</div>
<div class="mdl-dialog__content">
    {{night_action_text}}
</div>
<div class="mdl-dialog__actions mdl-dialog__actions--full-width">
    {% for c in candidates %}
    <button type="button" class="mdl-button close" onclick="{{js_function}}('{{game_id}}', '{{c.sid}}')">{{c.name}}</button>
    {% endfor %}
</div>