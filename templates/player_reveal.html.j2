<div class="common-font">
        Your role is {{player_role}}

    {% if known_word is not none %}<h5>Word: <u>{{known_word}}</u></h5>{% endif %}
    {% if image is not none %}<center><img class="role_image" src="{{ url_for('static', filename='img/' + image_theme + '/' + image) }}"
            title="{{role_description}}" width="200" height="200"></center><br> {% endif %}

    {% if known_players|length %}
    <p>Players who revealed themselves to you in the night:
    {% for p in known_players %}
    <br><b>{{ p.name }} {% if p.name == mayor %}, the Mayor, {% endif %} is </b> role: <b>{{ p.role }}</b><br>
    {% endfor %}
    {% endif %}
    <button type="button" class="btn btn-primary close" onclick="ack_reveal()">OK</button>
</div>


{# Bootstrap version of the above... is it just me or does it look more verbose by far? #}
{# <div class="modal" tabindex="-1">
        <div class="modal-dialog">
                <div class="modal-content">
                        <div class="modal-header">
                                <h5 class="modal-title">
                                        Your role is {{player_role}}
                                </h5>
                                <div class="modal-body">
                                </div>
                                <div class="modal-body">

                                        {% if known_word is not none %}<h5>Word: <u>{{known_word}}</u></h5>{% endif %}
                                        {% if image is not none %}<center><img class="role_image"
                                                        src="{{ url_for('static', filename='img/' + image_theme + '/' + image) }}"
                                                        title="{{role_description}}" width="200" height="200"></center>
                                        <br> {% endif %}

                                        {% if known_players|length %}
                                        <p>Players who revealed themselves to you in the night:
                                                {% for p in known_players %}
                                                <br><b>{{ p.name }} {% if p.name == mayor %}, the Mayor, {% endif %} is
                                                </b> role: <b>{{ p.role }}</b><br>
                                                {% endfor %}
                                                {% endif %}
                                </div>
                                <div class="modal-footer">
                                        <button type="button" class="btn btn-primary close"
                                                onclick="ack_reveal()">OK</button>
                                </div>
                        </div>
                </div>
        </div>
</div> #}