<div class="mdl-dialog__content">
    The word was: {{word}}
</div>
<div class="mdl-dialog__content">
    {{voting_text}}
</div>
<div class="mdl-dialog__actions mdl-dialog__actions--full-width">
    {% for c in candidates %}
    <button type="button" class="mdl-button {% if vote == c.sid %}mdl-js-button mdl-button--raised mdl-button--accent {% endif %}close" onclick="vote_player('{{game_id}}', '{{c.sid}}')">{{c.name}}</button>
    {% endfor %}
</div>
<div class="mdl-dialog__content">
  Waiting for <br>{% for v in voters %} {{v}}, {% endfor %}
</div>